# Development/test-only dependencies. NOT part of runtime dependency truth:
# these are never installed into the Docker image and are excluded from the
# CPU/GPU locks. CI installs them after the locked runtime set.
pytest==8.3.4
pytest-asyncio==0.24.0
# Test-fixture generators (backend/tests/office_fixtures.py) build synthetic
# docx files; python-pptx/openpyxl already ship as runtime dependencies.
python-docx==1.2.0
# Synthetic PDF generation for tests and benchmark corpus tooling
# (deferred imports in app code, module-level in tests).
reportlab==5.0.0
